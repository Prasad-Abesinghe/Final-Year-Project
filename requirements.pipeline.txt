# Pipeline: blockchain + FL combined requirements
# Note: oqs is simulated locally via shield_gh_blockchain/client/oqs.py
cryptography>=41.0.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
jsonschema>=4.17.0
scikit-learn>=1.3.0
scipy>=1.10.0
# Flower — FL framework (torch installed separately in Dockerfile with CPU index)
flwr>=1.9.0
